# 一键睡眠打卡按个人作息重锚定

为保留“睡前按一次、睡醒按一次”的单快捷指令体验，未提交 `rest_type` 的休息事件不再永久按上一条类型取反：间隔严格大于 12 小时后，按北京时间 21:00–05:00 的个人入睡窗口重新判定类型；不足两分钟的自动重复请求拒绝写入。选择这一规则是为了将漏打卡的影响限制在单个缺失区间，同时不要求快捷指令增加动作选择；显式 `rest_type` 仍可覆盖自动判断。
